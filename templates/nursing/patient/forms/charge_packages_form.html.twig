<turbo-frame id="nursing-patient-tab"><div class="card border-0 shadow-sm"><div class="card-body">
<h6>Cargar paquetes</h6><p class="text-muted">Ingresa JSON con paquetes.</p>
<form method="post" action="{{ path('app_nursing_patient_charge_packages', { id: admission_id }) }}">
<input type="hidden" name="_token" value="{{ csrf_token('nursing_charge_packages') }}">
<textarea name="items" class="form-control" rows="6">[{"id":1}]</textarea>
<div class="d-flex justify-content-end mt-2"><button class="btn btn-primary">Cargar</button></div>
</form></div></div></turbo-frame>
